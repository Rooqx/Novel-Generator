---
name: finalize-manuscript
description: Review a substantially complete manuscript and produce or execute a prioritized revision plan.
argument-hint: "[optional priorities]"
---

Invocation arguments: `$ARGUMENTS`

Read and follow `.agents/skills/finalize-manuscript/SKILL.md`. Treat that shared file as the canonical workflow.

When the workflow calls for a named specialist, use Claude Code's `Agent` tool with the exact role name when delegation materially helps. Otherwise read the canonical role file and perform the work directly.
