---
name: write-chapter
description: Draft the next chapter or a specified chapter while preserving continuity and established voice.
argument-hint: "[chapter number or constraints]"
---

Invocation arguments: `$ARGUMENTS`

Read and follow `.agents/skills/write-chapter/SKILL.md`. Treat that shared file as the canonical workflow.

When the workflow calls for a named specialist, use Claude Code's `Agent` tool with the exact role name when delegation materially helps. Otherwise read the canonical role file and perform the work directly.
