---
name: plan-novel
description: Create or substantially revise the novel premise, structure, chapter plan, character arcs, and required world foundations.
argument-hint: "[premise or planning constraints]"
---

Invocation arguments: `$ARGUMENTS`

Read and follow `.agents/skills/plan-novel/SKILL.md`. Treat that shared file as the canonical workflow.

When the workflow calls for a named specialist, use Claude Code's `Agent` tool with the exact role name when delegation materially helps. Otherwise read the canonical role file and perform the work directly.
